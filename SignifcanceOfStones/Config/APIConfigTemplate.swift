import Foundation

/// TEMPLATE: Do not use this file directly. Copy this file to APIConfig.swift and replace the placeholder with your API key.
/// Configuration for API keys and other API-related settings
enum APIConfigTemplate {
    /// The OpenAI API key used for AI completion requests
    /// Replace this placeholder with your actual OpenAI API key
    /// Important: Never commit your real API key to version control
    static let openAIKeyExample = "YOUR_OPENAI_API_KEY_HERE"
}

/* 
 INSTRUCTIONS:
 1. Make a copy of this file and name it "APIConfig.swift"
 2. Change the code in your copy to:

 import Foundation

 enum APIConfig {
     static let openAIKey = "YOUR_ACTUAL_OPENAI_API_KEY"
 }
 
 3. Replace the placeholder with your actual OpenAI API key
 4. The .gitignore is set up to prevent APIConfig.swift from being committed
*/ 